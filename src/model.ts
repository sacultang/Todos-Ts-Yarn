export interface Todo {
  id: number;
  todo: string;
  isDone: boolean;
}

// const RedcuerExample = () => {
//   const [state, dispatch] = useReducer(TodoReducer, []);
//   return <div>model</div>;
// };

// export default RedcuerExample;
